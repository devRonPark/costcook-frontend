// sort, order 상수화

// sort
export const SORT = {
    CREATED_AT : "createdAt",
    AVG_RATINGS : "avgRatings",
    VIEW_COUNT : "viewCount"
};

// order
export const ORDER = {
    ASC : "asc",
    DESC : "desc"
}

